# 12 - DeepSeek dry-run maps

Local eval against `deepseek-v4-flash` while OpenRouter `:free` is 429.
Not the ticket 12 Nemotron bar. Replay Nemotron after UTC midnight into
`12-live-maps` / `12-live-gate.md`.

```
node --env-file=.env .scratch/first-principled-v6/research/12-deepseek-eval.mjs
```

Each concept becomes `<concept>.json`. No secrets. No prompt rewrite.
Prod stays on OpenRouter Nemotron `:free`.
