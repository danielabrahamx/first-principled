# 01 - Ship the inherited map-first concept tree

**Type:** task (AFK)
**What to build:** the concept tree as the home page, live. Commit the
uncommitted v2 map-thread work sitting in the working tree (map-first layout
with docked Tutor pane, clickable node panels, hover history + layer stories,
timeline scrubber + replay, turn ledger), run the live verification pass on
real DeepSeek (billing topped up), re-run the pending 375px viewport check,
and deploy to first-principled.netlify.app.

**Blocked by:** None - can start immediately.

**Status:** ready-for-agent

- [ ] v2 map-thread code committed and pushed (scoped add, never git add -A)
- [ ] Live init + turns verified on real DeepSeek end to end
- [ ] 375px viewport verified (the pending re-run from v2 ticket 09)
- [ ] Deployed to prod, key-leak grep clean
